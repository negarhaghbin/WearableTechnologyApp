<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorForeground">@color/foreground_material_light</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- neutral application theme-->
    <style name="neutral_theme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/neutralPrimary</item>
        <item name="android:colorPrimaryDark">@color/neutralPrimaryDark</item>
        <item name="android:colorAccent">@color/neutralAccent</item>
        <item name="android:titleTextAppearance">@style/neutral_theme_title</item>

    </style>

    <style name="neutral_theme_title" parent="TextAppearance.AppCompat">

        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/neutralPrimaryDark</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">@style/Theme.AppCompat.Light</item>
        <item name="android:textAllCaps">true</item>


    </style>

</resources>
